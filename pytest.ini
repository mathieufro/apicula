[pytest]
testpaths = tests
markers =
    heavy: exercises a real 34 MB bitstream (smoke self-tests, c2 round-trip,
        calibration) -- excluded from the fast gate scope (P0.T42, ~90 s
        budget), included in full/all.
