"""Namespace package root for `fuzz.gw5ast138c.harness` (P0.T18).

Exists only so the harness is importable as `fuzz.gw5ast138c.harness.<module>`
from `$FL/apicula` regardless of cwd. No harness logic lives here.
"""
