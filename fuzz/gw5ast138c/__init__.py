"""Package root for the GW5AST-138C fuzzing harness (P0.T18).

Sub-packages: `harness/` (the driver modules, `spec-harness.md` §1) and
`shapes/` (one file per fuzzing shape, append-only for Phases 1-5b).
"""
